# 阶段 1B 验收清单（实现前冻结）

版本标识：`STAGE-1B-0.1.2`  
HEX：`releases\Stage1B.hex`（命令行产物）  
SHA256：`6928DA54876CC3E95766DA4172FDDC2E26CBD175A0B96CEDD62722C0EE0B2AF8`  
用户验收：2026-09-07 15:29「与预期一致」；15:31「阶段 1B 通过」。接受版本 `STAGE-1B-0.1.2`。  
占用：data=28.3，xdata=171，code=5897；C51 0 error / 0 warning  
链接：LX51 1 warning（未调用 `disp_test_step`），0 error。`TouchKeyRestart` 已链接。OMF2 + CODE 从 0x80 起，触摸向量在 0x005B。  
链接：须用 **LX51**（工程已设 `useL251=1`）。μVision 若仍走 BL51，会报 `L218: NOT AN OBJECT FILE`（该 `.LIB` 不是 BL51 目标文件）。  
改工程后请先关闭再打开 `ADB-Cursor.uvproj`，再 Rebuild；不要把内存里的旧工程另存覆盖。  
适配：Sense_Lib 为 LARGE 模型，工程改为 LARGE；库文件因链接器文件名解析改名为 `SC95F8X6X_HighSensitive.LIB`（内容 SHA 不变）。  
基线：已验收 `STAGE-1A-0.1.2`

## 输入

- 阶段 1A 通过的工程、DispUart、Timer0、TM1640 段码表。
- 授权例外：仅从 `D:\嵌入式项目文件夹\ADB` 复制 `Sense_Lib` 及该库运行必需的触摸配置。
- 允许资料：官方头文件/手册、原理图、电控规格书（按键/蜂鸣相关页）。
- 不读取旧工程 HMI、按键业务、显示逻辑、整板初始化、通信协议。

规格书冲突（实现前发现，已改冻结）：负载表写明「按任意按键蜂鸣器都无蜂鸣声，只有上电时会蜂鸣一声」。A1B-05 按规格书验收上电蜂鸣，不按键蜂鸣。

## 本阶段不做

本地设定闭环（1C）、电源板协议、红外、涂鸦、OTA。  
不改芯片 Option / 烧录器配置。

## 交付物

- 已登记来源的 Sense_Lib 副本（仅触摸采集/初始化/通道配置）。
- 按键事件日志（可区分通道或键名）。
- 上电蜂鸣可听验证（规格：按键不鸣）。
- 编译 HEX、校验值、复制范围记录。

## 验收项

| 编号 | 行为 | 方法 | 预期 |
| --- | --- | --- | --- |
| A1B-01 | 复制范围 | `docs/sources.md` | **通过**（仅 Sense_Lib 四文件） |
| A1B-02 | 编译 | `scripts/build.ps1 -Rebuild` | **通过**（0 error；L57 仅 `disp_test_step`） |
| A1B-03 | 日志回归 | COM7 115200 | `STAGE-1B-*` → **通过**（`STAGE-1B-0.1.2`，`done` 递增） |
| A1B-04 | 按键 | 实板按键 + 外部日志 | **通过**（`KEY tk=8\|10\|18\|21\|23\|28`） |
| A1B-05 | 蜂鸣 | 耳听 | **通过**（P2.4 上电短鸣；按键不鸣） |
| A1B-06 | 口隔离 | 源码 | **通过**（仍不初始化电源板/WiFi UART） |
| A1B-07 | 显示 | 目视 | **通过**（上电显示 `06`；按下显示通道号） |

## 不做的验收缩水

不以库自检打印代替按键实板。键名若规格书未给出，日志用通道号，不从旧工程抄键表。
