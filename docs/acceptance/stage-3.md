# 阶段 3 验收清单（实现前冻结）

版本标识：待实现（`STAGE-3-*`）  
基线：已验收 `STAGE-2-0.1.2`（2026-09-07 17:05「阶段 2 通过」；A2-06 失联搁置）

## 输入

- 阶段 2 通过的工程（HMI、触摸、显示、电源板 4800 收发、DispUart）。
- 允许资料：`D:\格力空调ADB\红外遥控器协议.doc`（已提取载波/系统码/字节定义）、电控规格书中与红外/未做本地功能相关的页、8763 手册中红外脚位。
- 不读取旧工程红外解码、遥控状态机、整板初始化。

## 本阶段不做

涂鸦 / WiFi UART、OTA、产测、品牌 PID、改 Option。  
阶段 2 搁置的失联 `--` / `PWR lost` 本阶段仍不重开，除非你明确要求。  
掉电 IAP 记忆仍不做（规格「断电前记忆」留给后续，避免本阶段改 IAP/Option）。

## 协议要点（已从授权红外协议 doc 提取，实现时按此编码）

- 载波 **38 kHz ±1%**。
- 帧：引导码 + 系统码 + 识别码 + 校验码 + 尾码；数据 **120 bit**，第 121 位为尾码。
- 系统码 **`01010110B`（`0x56`）**。
- 识别码字节 **A…M**，校验字节 **N**：系统码与识别码按文档做 4 位加和。
- 发送位序文档同时出现「高位先」与「低位先发」，**实现时以该 doc 正文为准并在日志里写明采用的一种**；不从旧工程抄位序。
- 与 1C 已有功能的对照（本阶段要接到同一套 RAM 设定）：
  - A7–A0：设定温度；16℃=`01101100` 起。
  - D6–D4 模式：001 制冷 / 010 制热 / 011 除湿 / 101 送风（100 自动：本机无自动模式，收则忽略并打日志）。
  - D1–D0 风速：00 关 / 10 低 / 11 中 / 01 高（红外无超快；超快保持本机键循环，红外不写入 turbo）。
  - E7–E6：开/关机。
  - 定时开/关相关位（D2/D3、H/I/J/K）映射到现有 0–24 h 定时；映射不清的位只打日志。
- 红外温度范围文档为 **61–90°F / 16–32°C**。本机按键范围仍是 1C 的 61–88°F / 16–31°C；红外超出则 **钳位** 到本机范围，不放宽按键限位。
- 摆风、睡眠、ECO、WiFi、童锁、GEN、L-GEAR、电辅热等 **本机无对应键** 的位：只日志、不发明新 UI。
- 室内环境温度在 G 字节（实测值+20）：面板无室内 NTC，**不把该字节当作本机 NTC**。

红外输入脚：提示词未给。实现时用授权原理图 + 8763 手册确定，确认后再初始化该脚；未确认前不猜、不抄旧工程脚位。

## 冻结的产品决策（阶段 3）

| 项 | 本阶段取值 |
| --- | --- |
| 多入口 | 红外写入与面板按键同一套 RAM；随后电源板按阶段 2 规则继续发 `66 99` |
| 未支持键 | 自动模式、摆风、睡眠等只记日志 |
| 失联 | 仍搁置 |
| 掉电记忆 | 不做 |

## 验收项

| 编号 | 行为 | 方法 | 预期 |
| --- | --- | --- | --- |
| A3-01 | 编译 | `scripts/build.ps1 -Rebuild` | 0 error；警告须解释 |
| A3-02 | 日志回归 | COM7 115200 | `STAGE-3-*`；DispUart 仍可读 |
| A3-03 | 口隔离 | 源码 | 仍不初始化 WiFi UART |
| A3-04 | 红外开机/关机 | 实板遥控器 + 日志 + 继电器 | 与电源键同一套开关机 |
| A3-05 | 红外模式/风速/温度 | 目视 + `PWR tx` | 与 1C 循环及阶段 2 字节对照一致；超范围钳位 |
| A3-06 | 红外定时 | 目视 | 能映射则与面板定时一致；不能映射则日志说明 |
| A3-07 | HMI / 电源板回归 | 目视 + 继电器 | 面板按键与 4800 发送仍成立 |
| A3-08 | 蜂鸣 | 耳听 | 仍仅上电鸣 |

## 不做的验收缩水

不以「能看到红外波形」代替设定被改写。不把旧工程解码表当对照去抄代码。无确认脚位不得宣称红外已接好。
