# 整体阶段计划

顺序按提示词建议，不提前实现后续功能。每阶段需你回复“阶段 N 通过”后才进入下一阶段。

| 阶段 | 目标 | 可观察交付 | 本阶段不做 |
| --- | --- | --- | --- |
| **-1** | 新工程、最小 Blink/Hello、编译烧录运行 | HEX、版本串、独立日志抓取、验收清单 | 触摸、HMI、电源板协议、红外、涂鸦、OTA。**已通过。** |
| **0** | 硬件资源、时基、日志、最小驱动 | 脚位表冻结、时钟/WDT 实测、安全 GPIO | 业务协议。**已通过。** |
| **1A** | 显示与段码 | 数码管/指示灯实测表 | 按键业务。**已通过。** |
| **1B** | 触摸库适配、按键与蜂鸣 | 按键事件与蜂鸣（Sense_Lib 复用） | 电源板联调 / HMI 设定。**已通过。** |
| **1C** | 本地 HMI 闭环 | 本地设定与显示一致 | 电源板协议。**已通过。** |
| **2** | 电源板通信与异常 | 4800 口收发与失联行为 | 涂鸦 / OTA。**已通过** `STAGE-2-0.1.2`（A2-06 搁置）。 |
| **3** | 其余本地功能与红外 | 红外与剩余本地功能 | 涂鸦 / OTA。**已通过** `STAGE-3-0.1.3`（A2-06 仍搁置）。 |
| **4** | 涂鸦 WiFi 与多入口一致 | 官方协议联调、三入口状态一致 | OTA / 掉电记忆。**已通过** `STAGE-4-0.1.3`（A2-06 仍搁置）。 |
| **4C** | 掉电记忆 | 断电后来电恢复约定设定 | OTA / 改 Option。**已通过** `STAGE-4C-0.1.0`。 |
| **4B** | MCU OTA | Boot 驻留 + DL→RUN；`releases\factory.hex` / `Stage4B.hex` / `Stage4B_ota.bin` | 真 A/B、LDROM Boot、读旧 Boot 源码。**已通过** `STAGE-4B-0.1.0`。 |
| **5** | 资源、回归、交付 | 占用、回归记录、最终报告 | — |

阶段 4 / 4C / 4B 已通过。下一阶段为 5（需你明确要求后再做）。A2-06 仍搁置。

OTA 仅在 4B 定向读授权 skill。触摸仅在 1B 按例外复制 Sense_Lib。
